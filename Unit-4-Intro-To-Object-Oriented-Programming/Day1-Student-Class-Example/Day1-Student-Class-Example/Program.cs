namespace Day1_Student_Class_Example;

class Program
{
    // This is an application program 
    // means it instantiates and uses objects to perform processing 
    // Every application has exactly one method called Main()
    
    static void Main(string[] args)
    {
        Console.WriteLine("Welcome to Student Class Example");
    }
    
    //
    //
    //
    
    // data - type name = initializer 
    
    // We need to be sure have all the data we want to store 
    // Before we instantiate the object
    List <int> scores = new List<int>();

    scores.Add(100);
    scores.Add(90);
    scores.Add(80);
    
    Student aStudent = new Student ("Frank, scores")

    public void ShowStudent()
    {
        Console.WriteLine("Name:
    }
}